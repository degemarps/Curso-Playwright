name: CI - Echo Test

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Echo message
        run: |
          echo "Olá, GitHub Actions! Este é um workflow de exemplo."